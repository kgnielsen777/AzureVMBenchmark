    # GitHub Pages Configuration
    # Auto-generated by Azure VM Benchmark Platform
    
    title: Azure VM Performance Benchmarks
    description: Comprehensive performance comparison of Azure VM series and versions
    theme: jekyll-theme-cayman
    
    # Markdown settings
    markdown: kramdown
    kramdown:
      input: GFM
      syntax_highlighter: rouge
    
    # Collections
    collections:
      results:
        output: true
        permalink: /:collection/:path/
    
    # Defaults
    defaults:
      - scope:
          path: ""
        values:
          layout: default
    
    # Exclude from processing
    exclude:
      - README.md
      - .gitignore
      - .git
      - vendor
      - node_modules
